import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF00B9E4);       // Light blue
  static const Color darkBlue = Color(0xFF1A3A6B);      // Navigation/text
  static const Color skyBlue = Color(0xFF87E8FF);       // Cards, hover
  static const Color teal = Color(0xFF00B4AA);          // Pills/tags
  static const Color indigoGray = Color(0xFF475A7D);    // Secondary text
  static const Color white = Color(0xFFFFFFFF);         // Background
  static const Color lightGray = Color(0xFFF2F4F8);     // Containers
  static const Color mediumGray = Color(0xFFC8D1DC);    // Inputs/disabled
  static const Color darkGray = Color(0xFF4B5563);      // Paragraph text
  static const Color success = Color(0xFF38B000);       // Taken
  static const Color warning = Color(0xFFFFA500);       // Upcoming
  static const Color error = Color(0xFFE63946);         // Missed
}
